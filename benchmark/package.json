{
  "name": "fast-xml-parser-benchmark",
  "version": "0.0.1",
  "private": true,
  "description": "Performance test",
  "scripts": {
    "parser": "node XmlParser.mjs",
    "builder": "node XmlBuilder.mjs",
    "test": "npm run parser && npm run builder && node try.mjs"
  },
  "devDependencies": {
    "benchmark": "^2.1.4",
    "xml2js": "^0.4.23"
  },
  "typings": "src/parser.d.ts",
  "dependencies": {
    "fast-xml-parser": "^3.21.1",
    "xmlbuilder2": "^3.0.2"
  }
}
